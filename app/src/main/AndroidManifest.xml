<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MiniProject">
        <activity android:name=".WelcomeActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    <activity android:name=".MainActivity" android:exported="true" />
    <activity android:name=".EnquiryActivity" android:exported="true" />
    <activity android:name=".UserResult" android:exported="true" />
    <activity android:name=".ProgrammeListActivity" android:exported="true" />
    <activity android:name=".DiplomaListActivity" android:exported="true" />
    <activity android:name=".DiplomaDepartmentsActivity" android:exported="true" />
    <activity android:name=".QualificationActivity" android:exported="true" />
    <activity android:name=".StpmEligibilityActivity" android:exported="true" />
    <activity android:name=".AboutUsActivity" android:exported="true" />
    </application>

</manifest>